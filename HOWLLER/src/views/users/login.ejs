<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;400;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" sizes="16x16" href="img/icons/howllericon-01.png">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/style_login.css">

    <title>Howller</title>
</head>
<body>
<!-- HEADER-->
<%- include('../partials/header') %>

<h4> Inicia sesión </h4>
        <div class="login-container">
            

            <form action="/users/login" method="POST">



                <div class="field-wrap <%= locals.errors && (errors.password || errors.email)  ? 'field-wrap-error' : null %>">
                    <input
                        type="email" 
                        name="email"
                        autocomplete="off" 
                        placeholder="Correo electronico*"
                        value="<%= locals.oldData ? oldData.email : null %>"
                    >

                </div>

                <div class="field-wrap <%= locals.errors && (errors.password || errors.email)   ? 'field-wrap-error' : null %>">
                    <input 
                        type="password" 
                        name="password"
                        autocomplete="off" 
                        placeholder="Contraseña"
                    >

                </div>

                
                <% if (locals.errors && errors.email) { %>
                    <div class="text-danger">
                        <%= errors.email.msg %>
                    </div>
                <% } %>



                <button type="submit" class="button-form">Ingresar</button>

            </form>

        </div>

        <h4> ¿Aún no tienes una cuenta? Regístrate <a href="/users/register">aquí</a> </h4>


</body>

<%- include('../partials/footer') %>

</html>